export class UserAgent implements IUserAgent extends EventEmitter {
    
    call(params:ICallParams): void{
        
    }; 
    answer(params:ICallParams): void{
        
    };
    reject(): void{
        
    }; 
    createDataChannel():IDataChannel{
        
    };
    init(configuration:IRTCConfiguration): void{
        
    }; 
}