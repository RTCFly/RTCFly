export default function(){
    return new Promise(function (resolve) {
        resolve([{
            deviceId:"deviceId",
            groupId:"groupId", 
            kind:"kind", 
            label:"label"
        }]);
    });
}